version '3'

services:
  app1:
    build: .
    environment: 
      - APP_NAME=app1
    Ports:
      - "3001:3000"

  app2:
    build: .
    environment: 
      - APP_NAME=app2
    Ports:
      - "3002:3000"
  
  app3:
    build: .
    environment: 
      - APP_NAME=app3
    Ports:
      - "3003:3000"